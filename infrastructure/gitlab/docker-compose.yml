services:
  gitlab:
    image: gitlab/gitlab-ce:17.4.6-ce.0
    container_name: gitlab
    hostname: gitlab.local
    restart: unless-stopped
    ports:
      - "8929:8929"
      - "5050:5050"
      - "2224:22"
    volumes:
      - ./config/gitlab.rb:/etc/gitlab/gitlab.rb:ro
      - ./data:/var/opt/gitlab
      - ./logs:/var/log/gitlab
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://192.168.0.200:8929'
        registry_external_url 'http://192.168.0.200:5050'
        gitlab_rails['registry_enabled'] = true
        registry['enable'] = true
        gitlab_rails['gitlab_shell_ssh_port'] = 2224
        gitlab_rails['gitlab_ssh_host'] = 'localhost'

    shm_size: '256m'
    deploy:
      resources:
        limits:
          memory: 4g
          cpus: '4'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://192.168.0.200:8929/-/health"]
      interval: 30s
      timeout: 10s
      retries: 5