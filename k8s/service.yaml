apiVersion: v1
kind: Service
metadata:
  name: task-manager-service
  namespace: pet-project
spec:
  selector:
    app: task-manager
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30080
  type: NodePort