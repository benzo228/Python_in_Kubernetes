apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: pet-project
data:
  postgresql.conf: |
    max_connections = 100
    shared_buffers = 128MB
    wal_level = replica
    max_wal_senders = 10
    wal_keep_segments = 32
    hot_standby = on